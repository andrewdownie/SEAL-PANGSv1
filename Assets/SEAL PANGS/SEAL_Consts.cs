
public static class SEAL_Consts {
	/*
	NOTE: these are the constant max/min's for what a character can actually have,
			not what an effect is limited to have. For example an effect can have greater than the max / min value,
			but it will result in the player hitting the max/min after it is applied to the player
	*/
	public const int MIN_ATTRIBUTE = 4;
	public const int MAX_ATTRIBUTE = 99;

	public const float MAX_ASPECT = 999;
	public const float MIN_ASPECT = 1;

	public const float MAX_COMBAT_STAT = 999;
	public const float MIN_COMBAT_STAT = 1;


	
}
